# Hotel Booking Microservices Environment Variables
# Copy this file to .env and fill in your values

# JWT Settings
JWT_SECRET_KEY=YourSecureSecretKeyMinimum32Characters!
JWT_ISSUER=HotelBookingAPI
JWT_AUDIENCE=HotelBookingAPIUsers

# Database Connection (Note: Using port 5433 to avoid conflict with local PostgreSQL)
DB_CONNECTION_STRING=Host=postgres;Port=5432;Database=hotel_booking;Username=admin;Password=YourDatabasePassword123!;SSL Mode=Prefer
DB_PASSWORD=YourDatabasePassword123!

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/YOUR-ACCOUNT-ID/hotel-notifications.fifo

# Redis Configuration
REDIS_PASSWORD=YourRedisPassword123!

# Environment
ASPNETCORE_ENVIRONMENT=Production

# Optional: Email Service (if using)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Note: PostgreSQL is accessible on localhost:5433 (instead of default 5432)
# to avoid conflicts with existing local PostgreSQL installations
